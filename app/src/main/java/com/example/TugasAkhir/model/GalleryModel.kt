package com.example.TugasAkhir.model

class GalleryModel {
    var gambarMenu:Int ? = 0
    var namaMenu:String ? = null

    constructor(gambarMenu: Int?, namaMenu: String?) {
        this.gambarMenu = gambarMenu
        this.namaMenu = namaMenu

    }
}